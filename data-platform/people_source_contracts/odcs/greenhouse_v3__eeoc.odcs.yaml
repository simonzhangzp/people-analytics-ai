apiVersion: v3.0.2
kind: DataContract
id: urn:people:bronze:greenhouse_v3:eeoc
info:
  title: eeoc
  version: 1.0.0
  description: Application grain. Never join worker/person.
servers:
- server: people_bronze
  type: s3
  path: people_bronze/greenhouse_v3/eeoc/extract_date={date}/run_id={id}/
schema:
- name: eeoc
  physicalType: parquet
  physicalName: people_bronze/greenhouse_v3/eeoc
  primaryKey:
  - application_id
  description: Application grain. Never join worker/person.
extract:
  mode: incremental
  cadence: daily
  key: application_id
  control_total_source: Harvest list total
  watermark: updated_at
slaProperties:
  freshnessHours: 24
piiClassification: restricted
