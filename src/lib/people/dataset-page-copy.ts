/** Bound from docs/PEOPLE_DATASET_PAGE_COPY.md. Keep the two in sync. */

export const DATASET_FRESHNESS_FROZEN =
  "The dataset is frozen at data-v1 (as-of 2026-08-31).";

export const DATASET_FRESHNESS_VERIFIED =
  "The dataset is frozen at data-v1 (as-of 2026-08-31); a daily job verifies the serving layer.";

export const HEALTHCHECK_STREAK_READY = 3;

export function datasetFreshnessCopy(status?: {
  consecutiveDays: number;
  lastRunDate: string | null;
} | null): string {
  if (
    status &&
    status.consecutiveDays >= HEALTHCHECK_STREAK_READY &&
    status.lastRunDate
  ) {
    return `${DATASET_FRESHNESS_VERIFIED} Last serving check: ${status.lastRunDate}.`;
  }
  return DATASET_FRESHNESS_FROZEN;
}

export const PEOPLE_DATASET_PAGE_COPY = {
  eyebrow: "Synthetic GlobalTech dataset — not a real company",
  title: "The workforce data behind People Analytics AI",
  lead:
    "Numbers on this site are generated by a seeded business-event simulator that emits Frappe HR and Greenhouse Harvest v3 documents. They are not a customer’s employees.",
  privacy:
    "GlobalTech is not a real company. Individual compensation, appraisals, EEOC, and survey items are not shown. Aggregates use cell-size suppression.",
  scenarios: [
    {
      id: "engineering_apac_attrition_rise",
      body:
        "Case 3. Voluntary attrition hazard raised for APAC engineers with 0–3 years tenure from 2026-03-01. Correlated (not causal): APAC Eng 0–3y compa-ratio lag and clustered manager changes from 2025Q4.",
    },
    {
      id: "apac_hris_feed_incomplete",
      body:
        "Case 2. One-day extract fault on 2026-08-14: APAC Employee rows_received = 35% of control_total. Not a workforce change.",
    },
    {
      id: "hiring_slowdown_hm_latency",
      body:
        "Case 4. Sales Onsite dwell ×1.6 and offer acceptance −8 points from 2026-05-01.",
    },
  ],
  synthetic:
    "E1–E7 plus E9 (rebalance_synthetic manager-change class). Compensation bands, grade rank, city geography, recruiter attributes, engagement instrument, separation-reason map, GlobalTech org names, and silver classification of month-end tree-repair manager changes.",
  external:
    "O*NET 31.0 occupation/skill files, BLS public series (including JOLTS quits used to calibrate attrition), Microsoft Learn catalog. Scale of the synthetic HR/ATS world is ~80–90k worker spells over 5 years, ending 49,823 certified.",
  freshness: DATASET_FRESHNESS_FROZEN,
} as const;
